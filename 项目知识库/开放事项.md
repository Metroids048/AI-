# 开放事项

## Gate 17 当前观察结果（2026-08-19）

- 启动与 Runtime Deployment 已通过：`ACTIVE / BINANCE_TESTNET / TESTNET_CANARY`，五币 Canary，`entry_authorized=true`，`startup_contract_errors=[]`，持续 `reconciliation=HEALTHY`。
- BTC 手动基线已按授权从 `short 0.5346` 安全刷新为 flat；未发现未解决 V2 intent、managed position、未知订单或未投影 fill。
- 60 分钟只读自然观察覆盖 236 个 Scheduler cycle，但没有自然 entry，因此未产生可验证的 `Intent -> Exchange Order -> Fill -> Protection -> Reduce-only Exit` 闭环。
- Gate 17 仍为 `NOT PASSED`。证据：`docs/evidence/automated_trading_v2/natural_cycle_20260818T190030Z.json`；不得用手工订单、验收捷径、直接 cycle、mock、强制入场或放宽策略 gate 替代。
- 策略尸检与 Replay 继续允许只读推进；策略 live deployment 继续阻塞，直到真实自然生命周期证据出现且候选 Replay 通过。

## Gate 16 手动仓位兼容（2026-08-04）

- 已关闭：Testnet 契约验证错误要求账户空仓、补偿清理可能误平手动仓位的问题。
- 当前行为：手动/外部仓位保留为未托管基线；契约只管理本次增量并恢复基线。
- 仍开放：Gate 17 自然 Scheduler 证据与策略有效性验证；Gate 16 成功不得替代它们。

## Gate 17 方向隔离复验（2026-08-06）

- 已修复：V2 Testnet sampling fallback 在 active、持久化事实链中可以提交真实 Entry；Shadow、local Paper、未持久化运行仍不会写交易所。
- 已修复：捕获的外部基线只允许同方向增量，反向信号在 Binance 单向持仓模式下 fail-closed，避免误减/反转手动仓位。
- 实证：官方 60 分钟自然观察器已在 `v2_active`、`BINANCE_TESTNET`、`reconciliation=HEALTHY` 下运行；期间 BTC/ETH 真实候选均为多头或无信号，未出现可安全叠加到现有空头基线的自然 Entry，因此 `GATE 17 NOT PASSED`。
- 证据：`docs/evidence/automated_trading_v2/natural_cycle_20260805T172437Z.json`；外部基线保持 `BTC/USDT:short=0.5302`、`ETH/USDT:short=6.814`。
- 下一步：保持 active Scheduler 正常运行，等待同方向空头自然信号，完成真实 Entry、保护单、自然 ReduceOnly Exit、本地 `CLOSED` 与基线恢复；不得用手工订单、直接 cycle、mock 或清理外部仓位替代。

## Gate 17 观察授权修复（2026-08-06）

- 已修复：`-EnableNaturalTestnet` 启动路径现在显式设置只读 Gate 17
  观察授权；默认 Shadow 启动会清除该变量，不改变任何下单权限。
- 已验证：重启后 API/Scheduler 为 `ACTIVE/BINANCE_TESTNET`、入口已启用、对账
  `HEALTHY`；17:45 和 18:00 UTC 的真实候选均为多头，方向隔离正确拒绝，
  没有写入交易所。
- 当前仍开放：官方 8 小时观察器正在等待同向空头候选；在真实自然闭环证据
  出现前，Gate 17 不得标记完成。

## 来自 USER_KNOWLEDGE_BASE / 本地

- 核对本项目与主知识库状态标签（OPEN / USER_REPORTED / VERIFIED）
- 补全真实验收证据链接（测试输出、commit）
- 敏感边界：交易密钥、真实订单、收益数据 — DO_NOT_PUBLISH

## Git

保持主分支可构建可验证

## 策略内核重构 Gate 0/1（2026-07-30）

- 状态：`DATA_READY / ACTIVE_STRATEGY_REJECTED`。
- 已完成：BTC/ETH 五周期从 2023-01-01 连续覆盖至 2026-07-29，十条序列零 gap；Final Holdout 2026-01-29..2026-07-29 已冻结且未读取结果。
- 当前阻断：legacy candidate 的 1,126 笔组合 replay 为 Sharpe -0.1096、PF 0.9882、MaxDD 77.14%、净期望 -0.000202，四项晋级门槛全部失败；当前 CI 仍为 IID percentile，且回放/实时 next-bar parity、funding/spread/latency/partial-fill 成本尚未完成。
- 下一步：按既有策略 V2 计划进入 Phase 1 数据/特征一致性与 next-bar parity；不得调整策略或晋级门槛，不得用 Final Holdout 调参。

## V2 Gate5（2026-07-29）
- 状态：BLOCKED（诚实，无伪造 NATURAL 证据）
- 代码：ACTIVE 事实链 / 无盲 TIME_EXIT 已接线
- 阻塞：utomated_trading_engine 仍为 legacy；需 2_active + runtime ntry_enabled=true + 自然 closed-bar 观察

## V2 Gate5 更新（2026-07-30）

- 上述 `BLOCKED` 状态已被真实自然 Scheduler 证据取代：Entry/Protection/Exit/Final Reconciliation 闭环 `VERIFIED`。
- 待人工复核：1 条历史 `PROTECTION_RECOVERY_FAILED` incident 仍为 `OPEN`；不得用健康对账自动抹除非 mismatch 类型事故。
- 下一阶段：Strategy readiness 与量化优化。先完成数据覆盖、lookahead/next-bar、费用/滑点/funding、walk-forward/OOS/bootstrap/置信区间检查，再讨论指标参数；不得把 5%/40x Testnet 采样配置带入 Mainnet。

## Strategy Readiness（2026-07-30）

- 报告：`docs/audit/strategy-readiness-2026-07-30.md`
- 结论：数据覆盖 `PASS`，当前 active candidate `REJECTED`，策略优化方法学 `NOT_READY`。
- 权威 corrected baseline：`artifacts/strategy_refactor/baseline-20260729-0000Z-r4`；旧目录因 JSONL 格式缺陷保留，`r1` 为同步前 source hash，`r2` 被并发 research-candidate commits 使 source hash 失效，`r3` 早于强制生成的 pytest 状态刷新，均保留但 superseded。
- 剩余方法学缺口：回放/实时特征与 next-bar parity、point-in-time funding/spread/latency/partial fill、逐窗口 walk-forward ledger、dependent/block bootstrap。
- 禁止为了开单或晋级改 MACD/RSI、5%/40x、止损止盈、net-edge 或门槛；Final Holdout 继续封存。

## V2 最终 drain（2026-07-30）

- 最新 BTC/ETH 自然保护退出已复验完成，终态为 Binance positions/open orders `0/0`、local open positions `0`、reconciliation `HEALTHY`。
- 入场开关已恢复为 `entry_enabled=true`；V2 工程闭环不再是开放阻断。
- 后续开放项仅保留 Strategy Readiness 的历史数据覆盖与科学验证缺口；Testnet 工程闭环完成不等于策略盈利或 Mainnet 就绪。

## Strategy Phase 1 parity（2026-07-31）

- 已完成首个窄切片：replay 的 closed-bar signal 改为 next-bar-open fill；末根无后续 bar 时不生成虚假成交。
- 验证：定向 replay `14 passed`，相关 validation `21 passed`；另补齐 `end_at` 跨窗口成交与窗口内强制收盘边界。
- 注意：`baseline-20260729-0000Z-r4` 现在是 pre-parity 历史基线，不能再宣称绑定当前 source hash；未重跑 baseline，未读取 Final Holdout。
- 下一步：MarketSnapshot/feature parity、input hash、point-in-time 成本、walk-forward ledger、dependent/block bootstrap。

## Strategy Phase 1 闭环后的开放项（2026-07-31）

- Phase 1 共用提案流水线、点时 Funding、八窗口独立回放和 append-only ledger 已完成，当前仍为 `NO_ACTIVE_STRATEGY`。
- spread、latency、partial fill 仍是显式 `ASSUMED`，在获得真实观察前继续阻断晋级。
- DSR/PBO、dependent/block bootstrap、候选调参、Final Holdout 和 Gate 17 均未执行，属于后续阶段。
- Gate 16 只证明 Testnet 订单契约，不能作为策略有效性或自然调度证据；引擎继续保持 `v2_shadow`。

## Fade / Reversion 严格 OOS（2026-08-15）

- 状态：`RESEARCH_COMPLETE / NO_PROMOTION`。
- [事实] `failed_breakout_reversal_v1` 已接入既有 EventEdge 八窗口 walk-forward + OOS + LCB95；3,592 个事件但 8 个窗口均无训练 gate 通过，结论 `REJECTED_WITH_EVIDENCE`。报告：`artifacts/strategy_refactor/reports/2026-08-15-failed-breakout-reversal-edge.json`。
- [事实] `range_sweep_reversion_v1` 严格现有边界触碰与区间条件得到 0 个事件，结论 `INSUFFICIENT_DATA`。报告：`artifacts/strategy_refactor/reports/2026-08-15-range-sweep-reversion-edge.json`。
- [事实] 真实 STOP 结构复核没有发现严格“反向扫单+下一根确认”匹配；不得把该假设写成已验证根因。表：`artifacts/strategy_refactor/reports/2026-08-15-loss-structure-table.json`。
- [事实] Regime 权重与 Bollinger 两项消融均独立留档，未并入主结论，未修改执行链或 production authorization。
- 下一步：继续自然积累 Forward/Shadow 证据；若要继续研究，需先批准新的数据或假设范围，不得回到第七个趋势变体或放宽 gate。

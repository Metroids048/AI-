# 当前状态

> 当前事实以代码、运行时数据库/调度状态和本文件为准。自然 candidate 尚未出现不构成工程阻塞。

## Repository

- 分支：`main` only
- 当前提交：Commit B（transaction contract refreeze）
- Dependabot vulnerability alerts：ON
- Dependabot automatic update branches：OFF

## Runtime

- 状态：`ACTIVE / BINANCE_TESTNET / TESTNET_CANARY`
- 执行范围：`BTC/USDT`、`ETH/USDT`
- `entry_enabled=true`、`entry_authorized=true`
- reconciliation：`HEALTHY`
- V2 critical task：alive；decision stream 持续推进

## Funding Cost

- Binance mark-price `@1s` public WS 是 current funding producer。
- producer 将原始 `r` 与 exchange event timestamp 写入 canonical `market_extras`。
- 历史 funding backfill 仍可审计，但超过 `15s` freshness tolerance 时为 `STALE`，不得作为实时成本。
- 成本方向：`LONG -> effective = raw`，`SHORT -> effective = -raw`；不使用 `abs()`。
- Production：`MISSING` / `STALE` fail-closed。
- Canary：`MISSING` / `STALE` 保持 diagnostic entry semantics。
- Funnel evidence：`raw_funding_bps`、`effective_funding_bps`、`funding_observed_at`、
  `funding_age_seconds`、`funding_status`、`funding_R`。

## Transaction Freeze

- baseline：Commit A `d0a0d3c0d999b67d5b8cef68617f268373f094d4`
- protected-path hashes：已更新为 Commit A
- HEAD verification：PASS without approval
- approval 环境变量：已清除

## Production

- 状态：`PENDING`
- 本轮未改变策略 identity、Promotion Gate、R2 门槛、费用/滑点、leverage、sizing、SL/TP、exposure 或 execution universe。

## Real R2 Funding Evidence

`REAL_R2_FUNDING_EVIDENCE = WAITING_FOR_REAL_SIGNAL`

这表示当前没有自然 candidate/funding_R 证据，不是开放工程 bug，也不阻塞交易工程冻结。

## Closeout

`TRADING_ENGINEERING_FROZEN = YES`（仅在 Commit B 后的无 approval transaction verification 通过时成立）

## 2026-08-22 收益恢复双车道收口

- 已实现 `Profitability Recovery Loop`：Master Loop 保留 S0-S15 与 `MAX_GENERATION=2`，
  统一 Registry 候选竞赛，`testnet_sampling_v2` 仅作为不可晋升 Canary 对照。
- 已实现严格 `ProfitabilityRecovery` gate、1.0x/1.25x/1.5x/2.0x 观测成本压力、
  Canary 单持仓 throttle，以及 `CHAMPION_PROPOSAL.json` 与双 Gate 报告输出。
- 当前没有新的 APPROVED Manifest，也没有本轮自然 Production Binance Testnet
  Entry/Fill/Protection/Reduce-only/0-0 reconciliation 证据；因此
  `EXECUTION_CHAIN=BLOCKED`、`PROFITABILITY_RECOVERY` 只能以真实候选证据决定，
  `overall_status` 不得写成 `FINAL_ACCEPTANCE_PASS`。
- 本轮 P0 窄修已验证：technical replay 从 `market_extras` 接入 point-in-time funding，
  Screening 与最终 Promotion 分层，Generation 0 写入 `GENERATION_0_PARTIAL.json`
  并复用 OHLCV/funding cache，Canary 强制 tiny diagnostic notional cap（50 USDT、
  单持仓、0.01 exposure），且不改变 `PAPER_RUNTIME_LIMITS` 或 V2 Binance 热路径。
- 验证事实：Master Loop/Canary 聚焦回归 32 passed；全仓 pytest 1825 passed、16 skipped、2 warnings；
  `ruff check .` PASS；touched-file mypy PASS；`git diff --check` PASS。
- 证据边界保持不变：没有 APPROVED Manifest、没有自然 Production Entry/Fill/Protection/
  Reduce-only/0-0 reconciliation，本轮不能宣称 Champion 或 Profitability Recovery 通过。

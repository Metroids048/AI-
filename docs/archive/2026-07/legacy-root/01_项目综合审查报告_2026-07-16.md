# AI Quant Research Platform — 综合审查报告（差距分析）

> 复核范围：`https://github.com/Metroids048/AI-` 全仓库
> 代码基线：`main@e2aec41`（最后一次提交 2026-07-15 18:28，含当天多轮"紧急调优"提交）
> 复核方式：逐文件通读 + 关键调用链追踪 + 与仓库自带的历史审计文档交叉核对，未连接你本地运行中的数据库/服务，因此不含"当前正在跑的进程实际状态"，只反映**代码现状**。

---

## 一、项目整体画像

这不是一个简化的"荐股/信号推送"工具，而是一个按 **Data / Strategy / AI Agent / Validation / Execution / Review** 六层架构组织的量化研究与自动交易平台，代码量真实、模块边界清晰，且有大量单元测试与历史审计文档做支撑（不是空壳/占位工程）。

核心交易通道现状：

| 通道 | 状态 | 说明 |
|---|---|---|
| BTC/USDT 永续资金费率 carry | **启用** | `AUTO_PAPER_RUNTIME_KEY`，今日刚完成"裸头寸→真正 delta-neutral 双腿对冲"重构 |
| Top20 技术方向性策略（4h方向/15m入场） | **启用（主力通道）** | `AUTO_PAPER_TECHNICAL_KEY`，10 类技术信号 + 多周期确认 + Ensemble + MetaLabel + LLM 否决 + 净期望值成本门禁 + Gatekeeper |
| 跨品种资金费率篮子 carry | **已实现但默认关闭** | `default_enabled_for_auto_trading=False` |
| Operator 4h/15m 经验策略 | **已实现但默认关闭** | 同上 |
| 中长期 swing（1d方向/4h入场，模块11） | **今日新增，明确标注"disabled research"** | 未经回测验证的新假设 |
| 信号观察通道（旁路成本门禁采样） | **今日新增，仅供人工 opt-in** | 不计入策略绩效 |

**结论先行**：这套系统"能不能自动交易"这件事本身没有问题——七层判定链（信号→多周期确认→Ensemble→MetaLabel→LLM否决→净期望值成本门禁→Gatekeeper风控）都是真实实现，不是桩代码。真正的问题集中在**判定链每一环的"通过率"被设计得非常严格，且今天的多轮调参分别独立地调整了不同环节，彼此之间没有对齐**，这也是本报告第七节和另一份《7x24自动开平仓逻辑详细报告》要重点拆解的内容。

---

## 二、后端模块盘点

### 2.1 API 层（`apps/api/routers/`，16 个路由模块）
`agents`（LLM任务）、`backtests`（回测/优化）、`console`、`ensemble`（信号融合/MetaLabel）、`ingestion`、`market` / `market_intelligence`、`metrics`、`notifications`、`research_sources`、`review`、`risk`、`runs`（Paper/Live 运行主入口，48K，是全仓最大的路由文件）、`strategies` / `strategy_library`、`system`。

覆盖面对应功能清单里的"研究入口/策略库/验证/执行/风控/复盘"六大模块，**没有发现明显缺失的顶层资源路由**。

### 2.2 服务层（`services/`）
- `data/`：行情摄取、Top20 universe、市场情报（新闻/宏观/社媒）
- `strategy_library/`：策略生命周期、Ensemble、MetaLabel 模型、技术指标库、Playbook/路线图
- `execution/`：`decision_pipeline.py`（决策管道）、`paper_signal.py`（订单生成）、`paper_runtime.py`（运行循环，2215行，全仓最大单文件之一）、`gatekeeper.py`（风控关口）、`gateway.py`（Binance 执行网关）、`carry_execution.py`、`scheduler.py`、`net_edge.py`、`signal_edge_stats.py`
- `agents/`：LLM 运行时（Anthropic/OpenRouter/GitHub Models 三路 fallback）
- `validation/`：历史回放、Exit策略对比（`technical_replay.py`）
- `review/`：复盘聚合（目前是规则聚合，非LLM总结，见第七节）

### 2.3 调度层
两种模式，由 `RUNTIME_SCHEDULER_MODE` 控制：
- `inprocess`：FastAPI 进程内 asyncio 定时任务（`services/execution/scheduler.py`），默认本地一键启动用
- `celery`：Celery Beat + Worker，`apps/api/celery_app.py` 中注册周期任务，生产多进程场景用

两者共用同一套 `PAPER_RUNTIME_CYCLE_SECONDS`（默认 300 秒）作为交易循环周期，调度机制本身**没有设计缺陷**。

---

## 三、前端模块盘点（`frontend/admin`，React + Vite + Tailwind）

已有页面：交易台（PaperConsole，首页）、风控台（RiskConsole）、策略库 + 策略详情、验证中心 + 回测详情 + 优化详情、复盘中心（ReviewCenter）、研究台（ResearchDesk）+ 研究源详情、运维控制台（OpsConsole）。

**判断**：作为内部运维/研究工具，页面覆盖度是合理的，不是"半成品前端"。

**已识别的前端缺口**：

1. **没有"决策漏斗/拒绝原因分布"可视化面板**。目前定位"为什么没开单"完全依赖运营手动写 Python 脚本查 `decision_snapshots` 表（本报告第八节、以及仓库里 `docs/diagnosis/system-readiness-2026-07-15.md` 都是靠手工脚本诊断的），OpsConsole 里没有把这类信息做成图表。这是当前"运维体验"最大的短板，也是 Q3 这类问题反复需要人工排查的直接原因。
2. `findings.md` 中记录过一个真实历史 bug：前端"杠杆滑块"从未真正驱动后端 `asset_risk_tiers` 生效值，档位预览是纯静态文案。该问题按记录已通过"方案B：滑块联动分档表"修复并有测试锁定（`tests/services/test_asset_risk_tiers.py`），但建议你在页面上手动验证一次杠杆滑块改动后"预览区域"数值是否真的随之变化，因为这类"表面存在但没接后端"的 UI 缺陷历史上出现过不止一次。
3. 没有 Prometheus/Grafana 前端集成或告警值班页面（`infra/grafana`/`infra/prometheus` 目录存在配置，但路线图 Phase 2 才计划做值班演练），目前系统健康状态只能通过 OpsConsole 的运行态卡片粗粒度查看。

---

## 四、外部第三方接入情况

| 类别 | 服务商 | 配置项 | 现状 |
|---|---|---|---|
| **交易所** | Binance | `binance_api_key/secret`、`spot_testnet_*`、`binance_usdm_rest_base` 等 | ✅ 已接入 Spot + USDM 合约，`binance_trading_mode=demo`（模拟盘）为主，`live_trading_enabled=False`（实盘总开关关闭）。执行严格遵循 "Binance-first"：本地账本不会在交易所未接受订单时先落地，避免账本分叉 |
| **LLM 决策否决** | Anthropic Claude / OpenRouter 免费模型 / GitHub Models 免费模型 | `claude_api_key`、`openrouter_api_key`、`github_models_token` | ⚠️ **三路 fallback 中两路（OpenRouter/GitHub Models）当前均报故障**：OpenRouter 免费模型 HTTP 429 限流、GitHub Models token 返回 401（权限范围不够）。Claude API key 虽已配置但**未被设置为否决 agent**（`AGENT_LLM_PROVIDER_MAP` 未指向 anthropic）。这是**高优先级问题**，详见本报告第七节 P0 与另一份报告的诊断部分 |
| **市场情报/衍生品数据** | CoinGlass、CryptoQuant、DeFiLlama | `coinglass_api_key`、`cryptoquant_api_key` | 已接入代码路径，需要用户自行配置 key 才能生效（key 默认空） |
| **宏观/新闻** | Trading Economics、Alpha Vantage、ForexFactory/金十/CoinDesk/TheBlock/Reuters/SEC EDGAR RSS | 对应 `*_api_key` / `*_rss_url` | 已接入轮询任务，多数 RSS 源免 key 可用，Trading Economics/Alpha Vantage 需要 key |
| **社媒** | Twitter（`twitter_bearer_token`） | 已接入 | 需要用户自行配置 token |
| **通知** | Telegram Bot、通用 Webhook | `telegram_bot_token`、`notification_webhook_url` | 已实现 outbox + dispatcher 重试机制 |
| **研究生态** | GitHub（`github_token`，用于研究源摄取）、Jesse/NautilusTrader/Qlib/vectorbt/OpenBB | 本地 manifest | 已摄取真实资产，不是空目录（此前有审计怀疑过"声称摄取但无文件"，已核实为误判） |
| **其他交易框架** | Freqtrade | `freqtrade_api_url` 等 | ❌ **代码中明确注释"declared but not yet wired"**（已声明但未接入），属于遗留配置 |
| **其他交易所** | OKX / Bybit | 无 | 路线图明确排除在当前阶段外，不算缺口 |

**第三方接入部分的核心风险**：不是"接得不够多"，而是**已接入的 LLM 否决链路两路免费供应商目前不可用**，而这一环在设计上是 **fail-closed**（任何超时/不可用/schema 校验失败都会强制 `veto=True`，直接拒绝交易）。这个点非常关键，会在报告二和 Q3 诊断中重点展开。

---

## 五、流程与数据闭环情况

### 5.1 研究到落地的闭环
`StrategyIdea -> StrategyDraft -> StrategyContract -> 回测 -> 样本外验证 -> 模拟盘准入 -> (未来)实盘`，这条链路在代码里是真实存在的持久化对象，不是文档里的口号。`AGENTS.md` 把这条链路固化为治理约束，`策略库/笔记.docx` 等研究素材明确只作为素材池，不能跳过这条链路直接变成"正式策略真源"——这个设计本身是自洽的。

### 5.2 单次交易决策闭环
```
调度器触发（默认300秒/次）
  → 拉取 Top20 各币种最新K线（15m入场/4h方向/1h状态，多周期）
  → 10类技术信号生成（不足则 fail-closed，不会伪造信号）
  → 多周期确认（方向周期与入场周期不一致则 fail-closed）
  → SignalEnsemble 相关性过滤 + 加权多数投票
  → MetaLabel 胜率/期望收益门禁（决定是否下注 + 仓位系数）
  → LLM 二元否决（只能否决，不能决定方向/价格/仓位；否决失败一律 fail-closed 强制否决）
  → 净期望值扣成本门禁（win_rate*avg_win - (1-win_rate)*avg_loss - 手续费 - 滑点 > 0）
  → Gatekeeper 规则风控（止损必须存在、Kill Switch、敞口/杠杆/相关性/回撤等数值风控）
  → 生成订单 → Binance-first 网关执行 → 本地账本落地
  → 持仓管理：保护性止损/移动止损优先于反向信号平仓，反向信号平仓优先于时间止损
  → 对账（reconcile）与复盘（Review Agent 聚合失败记录）
```
这条链路**没有发现绕过风控、无止损开仓、LLM决定方向/价格、或本地账本先于交易所落地的核心阻塞问题**（这一点与仓库自带的 `docs/audits/2026-07-12-current-state-review.md` 结论一致，我逐条代码复核后认同）。

### 5.3 数据闭环的真实缺口
1. **净期望值门禁默认吃"噪声代理"数据**：`services/execution/net_edge.py` 与 MetaLabel 的胜率/平均盈亏统计，默认来自最近约47根K线的原始收益率（`_meta_label_samples`），只有当 `artifacts/signal_edge_stats/<strategy_key>/active.json` 存在且未过期时才会切换成 `scripts/compute_signal_edge_stats.py` 离线生成的**真实历史回放统计**。**当前仓库里没有 `artifacts/` 目录**，也就是说这套"真实统计"从未被生成过，系统事实上一直在用噪声代理做生死判定。这是数据闭环里最大的一个"设计上存在，但从未真正启用"的缺口。
2. **决策快照（decision_snapshots）有落库但前端不可视化**，导致"为什么没开单"必须靠人工写脚本查询，运维闭环不完整（见第三节）。
3. Carry 策略今天刚完成 delta-neutral 双腿改造，**数据库迁移脚本（hedge_group_id 字段的 Alembic migration）和平仓时"同时平掉对冲组所有腿"的补充逻辑，按仓库自己的 `docs/audits/2026-07-15-task-execution-summary.md` 记录，都还没做**，测试也因依赖安装超时没跑过。这是一个**未闭环的半成品改动**，需要优先收尾，否则 carry 通道存在"主腿开仓成功、对冲腿逻辑不完整"的风险。

---

## 六、已完成能力（有代码证据，不是文档自称）

以下能力经过我逐文件核对源码，判定为**真实实现**：

- 10 类技术信号（MACD / 道氏理论趋势 / 价格行为含假突破 / RSI / EMA趋势 / ADX / VWAP / 布林带 / FVG缺口 / 多周期均线共振），比仓库 7-12 号审计文档记录的"8类"多了 `fvg` 和 `mtf_ma` 两类（7月新增）
- 4h方向 + 15m入场的多周期确认，且**今天刚完成"方向周期/入场周期使用不同信号子集"的拆分**（`direction_signals` vs `entry_signals`），修复了此前"两个周期共用同一信号集合"的已知缺口
- SignalEnsemble 相关性过滤（高相关信号对次级来源权重打4折）+ 加权多数投票（非全票一致，2026年已从AND-gate改为多数票+quorum）
- MetaLabel 下注决策 + 仓位系数计算，cold-start（历史样本不足）fail-closed
- LLM 二元否决，prompt 严格限定只能输出 `veto/veto_reason`，服务层做 JSON schema 强校验，三类失败（超时/供应商不可用/schema失败）全部 fail-closed
- Gatekeeper 规则风控：Kill Switch、强制止损、单品种/组合敞口、杠杆、日/周亏损、回撤、连续亏损、跨品种相关性簇敞口、净方向敞口，均有独立测试锁定
- 平仓优先级：保护性止损/移动止损 > 反向信号平仓 > 时间止损，同根K线内止损优先于止盈
- Binance-first 执行：有持仓时保护性平仓与反向平仓都先过交易所网关，网关不接受则不本地落地，避免账本分叉
- Carry 策略今日完成 delta-neutral 双腿对冲重构（原来是裸头寸冒充套利）

---

## 七、问题清单与优化建议（按优先级）

### P0（直接影响"能不能开单"，建议立即处理）

1. **LLM 否决链路两路免费供应商均不可用，且 fail-closed 会自动否决所有交易**
   - 现象：`system-readiness-2026-07-15.md` 记录 OpenRouter 429、GitHub Models 401
   - 影响：只要 `PAPER_RUNTIME_ENABLE_DECISION_VETO=true`（默认值）且 Claude 未被设为 veto agent，**每一次决策循环都会因为否决器不可用而强制拒单**，这是全局性的、会覆盖掉今天所有其他调参努力的问题
   - 建议：三选一——(a) 把 `AGENT_LLM_PROVIDER_MAP`/`AGENT_LLM_MODEL_MAP` 指向已配置的 Claude API 作为 veto agent（最推荐，稳定性最好）；(b) 修复 GitHub Models token 的 `models:read` 权限；(c) 临时设 `PAPER_RUNTIME_ENABLE_DECISION_VETO=false` 验证是否是这个环节导致零开单（仅用于诊断，不建议长期关闭风控否决）
   - 详见另一份报告与 Q3 诊断

2. **净期望值成本门禁长期依赖噪声代理统计**（`artifacts/signal_edge_stats/` 从未生成）
   - 建议：运行 `python -m scripts.compute_signal_edge_stats` 为主力策略生成真实历史回放统计，替换47根K线的噪声代理，这才是让"胜率/期望收益"判定回归真实、而不是靠反复调低阈值硬凑通过率的正确方向

3. **今天新增的 carry delta-neutral 改造尚未闭环**：缺 Alembic 迁移、缺"对冲组整体平仓"逻辑、单测未跑通
   - 建议：下一个会话优先收尾这三项，否则线上可能出现主腿有仓位、对冲腿状态不完整的风险敞口

4. **今天的多处调参彼此没有对齐**：`meta_label_min_win_rate` 从0.50下调到0.42（放宽准入）、仓位规则"ULTRA-AGGRESSIVE"式放大（风险占比5%/杠杆40倍/单笔仓位占比35%），但**这些改动都只影响"能不能通过判定"或"通过后仓位多大"，完全不触碰同样会致命的 LLM 否决 fail-closed 问题**。建议把"为什么不开单"当成一个漏斗问题，逐层量化通过率，而不是逐个环节盲改参数（详见 Q3 诊断）

### P1（影响交易质量与可维护性）

5. Review Agent 的"失败原因总结"目前是规则聚合，不是真正的LLM归因总结，复盘价值有限
6. 关键词匹配式 RAG（`services/agents/rag_context.py`），不是向量检索，扩展到更复杂研究问答场景前需要升级
7. 缠论（Chan theory）信号只有适配器骨架（`services/strategy_library/technical/chan_theory.py`），需要 vendor 第三方库 + 完整回测验证才能作为第9类信号真正接入
8. 中长期 swing（1d/4h，模块11）是全新未验证假设，标注"disabled research"，投入使用前需要独立样本外回测

### P2（工程债务/可观测性）

9. 全仓 65 个历史文件存在 `ruff format` 漂移（非阻塞，但建议单独立项批量修复，不与功能改动混在一起提交）
10. 缺少启动时配置自检（数据库连接、Binance key、LLM 供应商可用性等目前都是"运行到才发现失败"，不是启动即报错）
11. 缺 Prometheus/Grafana 值班告警演练（路线图 Phase 2 计划项）
12. 部分开发手册（如《AI量化平台_开发协作Prompt手册》）仍停留在"Phase 2暂不启动"的旧状态描述，与当前代码严重不符，容易误导后续开发者/AI助手，建议归档或更新

---

## 八、你现在最应该做的三件事

1. **立即确认 LLM 否决供应商状态**（P0-1），这大概率是当前"半天不开单"的最大嫌疑，且它是一个会让今天所有其他调参努力全部作废的开关级问题
2. **收尾今天未完成的 carry delta-neutral 改造**（P0-3），避免它在你不注意的时候产生风险敞口
3. **生成一次真实的 `signal_edge_stats` 离线统计**（P0-2），把净期望值门禁从"猜测代理"换成"真实回放证据"，这样以后再调阈值就有据可依，而不是像今天这样反复手动下调数字硬凑通过率

以上三点，我在另一份《7x24自动开平仓逻辑与量化策略详细报告》里给出了逐环节的代码级说明，并针对"半天没开单"给出了可以直接运行的排查脚本。

# 量化策略、开平单逻辑、仓位管理、LLM+RAG 详细报告

> 本报告逐层拆解当前系统的真实代码逻辑（而非设计文档的理想状态），并在每一层与你本人的
> 人工交易经验（4h 看方向 + 15m 假突破/假跌破入场、缠论买卖点、MACD 金叉死叉、RSI 超买超卖
> 多指标确认）做对照，指出"已经吸收了什么、还差什么、可以怎么补"。

---

## 一、总览：当前系统有两条独立的交易通道

系统目前**不是单一策略**，而是两条定位完全不同的通道，共用同一套 Validation/Execution/Risk/Review 骨架：

| 通道 | 定位 | 核心假设 | 成熟度 |
|---|---|---|---|
| **通道一：Funding Carry（资金费率套利）** | 跑通"数据→验证→执行→复盘"闭环的基石通道，风险最低 | 永续合约资金费率偏离时，扣除手续费/滑点后仍有正净边际 | 最成熟，回测指标真实、硬拒绝负期望样本 |
| **通道二：Technical Directional（技术方向性）** | 对应你本人的交易经验和习惯，直接决定 long/short | 4h 定方向 + 15m 找入场点，多指标共振 + LLM 否决过滤 | 已在 Binance Testnet 跑通开平单，是你接下来要优化的重点 |

你问的"量化策略、开单、平单、仓位管理、LLM+RAG"，主要都发生在**通道二**里，下面全部围绕通道二展开。

---

## 二、完整决策链路（从一根新 K 线到一笔订单）

```
新K线到达
  │
  ▼
① 技术信号生成（8个独立信号函数，各自判断多空，互不干扰）
  │  MACD / Dow趋势结构 / 价格行为(吞没·pinbar·Donchian突破·假突破假跌破) /
  │  RSI / EMA快慢线趋势 / ADX方向强度 / VWAP回归 / 布林带回归
  │
  ▼  （若市场情报开关打开，还会加入一票"受限投票"）
  ＋ Market Intelligence 信号（新闻/宏观/链上数据，权重硬顶30%，只是普通一票，不是否决）
  │
  ▼
② 多周期确认（对应你的"4h看方向、15m做入场"）
  策略配置 timeframe_model=4h_direction_15m_entry 时：
  15m信号方向 必须与 4h信号方向 一致，否则直接跳过本轮（reason=multi_timeframe_disagreement）
  │
  ▼
③ SignalEnsemble 信号融合
  对8个信号做"两两相关性检测"：相关度超阈值的一对，验证分数较低的一方权重打0.25折
  （避免多个指标本质在说同一件事时被重复计票）
  → 按 权重×置信度 加权投票，多方分/空方分谁大谁赢，得到融合方向+融合置信度
  → 若融合后没有明确方向，直接跳过（reason=ensemble_discarded）
  │
  ▼
④ MetaLabel 二级仓位判定（"信号对了，但值不值得下注"）
  用最近约47根已收线K线，按融合方向计算历史胜率和均值收益
  胜率 ≥ 阈值(默认0.48) 且 均值收益 > 0 → 判定"下注(bet_taken)"，并给出下注比例(0.1~1.0)
  否则 → 跳过本轮（reason=meta_label_bet_skipped），不下单
  │
  ▼
⑤ Decision Veto Agent（LLM 二元否决，这是唯一允许LLM参与执行链路的节点）
  只输出 veto:布尔值 + veto_reason:字符串，绝不允许输出方向/价格/仓位
  否决=true → 跳过本轮（reason=llm_veto）
  超时/预算超限/输出格式校验失败 → 强制判定为否决（fail-closed，宁可错过机会不可放宽风控）
  │
  ▼
⑥ 生成 ExecutionOrderRequest（带止损止盈计划、仓位、风险状态快照）
  │
  ▼
⑦ Gatekeeper 最终风控关口
  无止损→拒绝｜Validation未过关→拒绝｜数据不新鲜→拒绝｜有阻断性风险事件→拒绝
  ｜数值风控超限（敞口/杠杆/回撤/连亏/Martingale检测/API失败）→拒绝｜Kill Switch→全部拒绝
  │
  ▼
⑧ Binance-first 下单
  若开启 Testnet mirror：先提交 Binance Gateway，成功后才本地记录 fill/开仓；
  Gateway失败 → 本地不开仓，订单记为 rejected，避免本地账本和交易所账本分叉
```

这条链路的设计哲学与你的人工交易经验高度吻合的地方是**"多重确认才出手"**——你自己说的是
"4h定方向、15m找假突破/假跌破，同时看 MACD/RSI 辅助确认"，代码里对应的正是
"多周期确认 + 8个信号加权投票 + 历史胜率二次过滤 + LLM 最后把关"，只是把你脑子里的
"综合判断"拆解成了可回测、可审计的四道独立关卡。

---

## 三、开单逻辑逐项拆解

### 3.1 八个技术信号，与你的经验逐条对照

| # | 信号 | 代码实现要点 | 与你经验的对应关系 |
|---|---|---|---|
| 1 | **MACD** | 12/26/9 标准参数，先找最近7根内的金叉/死叉（`macd_bullish_cross`/`macd_bearish_cross`），若没有明确交叉则退化为看 histogram 动量方向（`macd_histogram`），用 `tanh` 归一化力度 | 完全对应你说的"MACD金叉死叉"，且多了一层"没交叉时看动量"的补充逻辑，比单纯看金叉死叉更连续 |
| 2 | **Dow趋势结构（道氏理论）** | 用局部窗口找 pivot 高点/低点，判断"更高的高点+更高的低点"=多头结构，反之=空头结构；结构不清晰时退化为"高低点斜率连续趋势强度" | 这是你说的"看多空方向"的结构化版本，本质是把"看图形结构"变成了可计算的 pivot 序列比较 |
| 3 | **价格行为（Price Action）** | 4个子信号：看涨/看跌吞没、pin bar（长影线）、Donchian通道突破、**假突破/假跌破**（`false_resistance_breakout_reversal` / `false_support_breakdown_reversal`）—— 判定逻辑是"最高价突破前高但收盘价收回前高下方，且上影线够长"才算假突破 | **这是与你经验最直接对应的一条**——"15min K线做假突破假跌破"在代码里就是这个 `generate_false_breakout_signal` 函数，逻辑与你描述的手法基本一致：破位后收回、影线拒绝确认 |
| 4 | **RSI** | 14周期，只在"从超卖(30)以下回升穿过30"或"从超买(70)以上回落穿过70"这种**明确的反转穿越时刻**才发信号，不是任何时候 RSI<30 就发信号 | 对应你说的"RSI超买超卖"，且做了"回升/回落穿越"过滤，避免了"RSI一直在超卖区但还在跌"这种常见误用 |
| 5 | **EMA快慢线趋势** | 20/50 EMA，看价差(spread)+50EMA斜率(slope)综合力度，力度低于阈值(0.0015)不发信号 | 对应趋势跟踪类指标确认，是你"多指标辅助判断"里的一环 |
| 6 | **ADX方向强度** | 14周期标准ADX+DI计算，ADX<22视为无明显趋势不发信号，DI+/DI-差值也要够大 | 这是过滤"盘整无方向"行情的关键指标，避免在震荡市强行找方向 |
| 7 | **VWAP回归** | 48根K线滚动VWAP，收盘价从下方站上VWAP=多头信号，从上方跌破VWAP=空头信号 | 短线成本锚点确认，属于机构常用但你原有经验里没有的补充维度 |
| 8 | **布林带回归** | 20周期2倍标准差，价格从带外收回带内=均值回归信号 | 属于震荡市辅助信号，与MACD/RSI等趋势类信号形成互补 |

**结论：你原有的经验（4h方向+15m假突破+MACD+RSI+多指标）已经被系统全部吸收，并且额外补充了
Dow结构、ADX强度过滤、VWAP、布林带四个你原来没有系统化使用的维度**——这是好事，代表系统在你的
基础上做了合理扩展，不是简单复刻。

### 3.2 唯一明显缺失的一块：缠论买卖点

你提到的"缠论的几类买卖点"（一买/二买/三买、一卖/二卖/三卖，以及笔、线段、中枢、背驰）**目前
在代码里完全没有实现**。这不是遗漏，是仓库文档里主动做出的决策：

> `策略库/04_风险控制与禁用清单.md`：
> "需要先定义客观算法：缠论一买、二买、三买、一卖、二卖、三卖。笔、线段、中枢、背驰。主观'盘感''强弱'描述。"

原因很直接——缠论的笔/线段划分本身在业界就有多种不同的客观化算法（分型判定标准、线段破坏条件、
中枢延伸规则等都有争议版本），如果不先把"用哪一种客观算法"钉死，直接让 LLM 或人工"感觉判断"，
就会破坏整个系统"规则化→可回测→可审计"的核心设计原则（这是 `AGENTS.md` 里"不可谈判项"之一）。

**如果你要把缠论加进来，建议的路径**（而不是直接写代码）：

1. 先选定一套**明确的、有公开定义的分型/笔/线段算法**（业界常见的比如"顶底分型+至少一根K线间隔
   构成笔"这类规则，需要你根据自己实盘验证过的具体判定标准来定，因为缠论确实存在多个流派差异）。
2. 把这套算法写成一份类似 `策略库/03_加密货币策略候选库.md` 里 S2/S3 那样的候选策略卡片：
   核心假设、入场条件、出场条件、需要的历史数据窗口。
3. 像现有 8 个技术信号一样，实现成一个独立的 `generate_chan_theory_signal()` 函数，输出标准的
   `TradeSignal`，接入 `enabled_signals` 白名单机制（现有信号选择机制已经支持这种插拔式扩展，
   不需要改动主链路代码）。
4. 先做**离线标注对照**（用历史数据跑一遍，看你人工标注的买卖点和算法输出的买卖点重合度），
   这也是文档里"离线标注"这一步的用意——先验证算法输出是否符合你多年人工判断的直觉，再决定要不要
   接入实盘信号池。
5. 接入后，它会自动参与 SignalEnsemble 的相关性过滤和加权投票，不需要单独开发融合逻辑。

### 3.3 多周期确认机制

当策略配置 `timeframe_model=4h_direction_15m_entry` 时，系统会用**同一套8个信号函数**分别在
15m（入场周期）和 4h（确认周期）上各跑一遍，取两边"信号方向得分"的多空胜出方，如果两个周期方向
不一致，直接放弃这一轮机会（`multi_timeframe_disagreement`）。

**需要你注意的一点**：目前"4h方向"和"15m入场"用的是**同一组指标函数**，只是喂进去的K线周期不同，
不是"4h专门看结构/趋势类指标，15m专门看假突破/反转类指标"这种更精细的分工。如果你希望更贴近自己
实盘的用法（比如"4h只看Dow结构和EMA顺不顺，15m专门盯假突破"），目前的代码结构可以支持——因为
`enabled_signals` 是可配置的白名单，理论上可以让确认周期和入场周期用不同的信号子集，但**当前实现
里确认周期用的信号集合和主周期是同一份**，这是一个可以优化的点（见第九章优化建议）。

### 3.4 SignalEnsemble 信号融合算法细节

- 每个信号有一个"基础权重"（MACD/Dow结构/EMA趋势权重最高 0.9~1.0，ADX 0.85，RSI/VWAP/布林带 0.75，
  价格行为类 0.7，市场情报信号权重硬顶 0.30），再乘以该信号自身的置信度(confidence)得到最终权重。
- 相关性过滤：把每个信号最近约80根K线的"方向化收益序列"两两算 Pearson 相关系数，相关度超过阈值时，
  验证分数较低的一方权重打 0.25 折——这是为了避免"MACD和EMA趋势本质在说同一件事，却被算成两票"
  的重复计票问题。
- 最终按加权投票得到融合方向（多方总分 vs 空方总分）和融合置信度（胜方得分占总得分的比例）。

### 3.5 MetaLabel 二级仓位判定

这一步在做的事情，用你熟悉的话来说就是——"信号说该开多，但这个信号最近的胜率怎么样，值不值得
下重手"。具体算法：

- 拿最近约 47 根**已收线**K线（严格排除未来数据），按融合方向计算"如果每根K线都跟着这个方向走，
  相邻两根之间的方向化收益率"。
- 算这组收益率的胜率（正收益占比）和均值。
- 胜率 ≥ 阈值（策略里配的 `meta_label_min_win_rate`，通道二默认 0.48）且均值收益 > 0，才判定
  "下注"，否则本轮直接放弃，不产生订单。
- 判定下注时，用"胜率超出50%的幅度 + 均值收益"算出一个 0.1~1.0 的仓位比例系数
  （`position_size_fraction`），这个系数会在后面的仓位计算里作为"置信度乘数"缩放实际下单金额。

**这里有一个你需要清楚知道的技术局限**：这不是一个训练出来的机器学习模型，而是一个"用最近一小段
历史数据现算胜率"的规则近似（代码里 `model_ref="rule_meta_label_v1"` 也直接承认了这一点）。
它的问题在于：
- 样本量小（47根），在盘整→趋势切换的转折期，用最近的历史胜率去判断"现在该不该下注"天然会滞后。
- 没有跨越更长历史、更多品种的训练和样本外验证，无法保证这个"48%胜率阈值"本身是经过统计检验的
  合理阈值，还是一个凭经验设的初始值。

### 3.6 Decision Veto Agent（LLM 唯一被允许介入执行链路的地方）

这是全项目对"LLM边界"控制得最严格、也最值得你信任的一块设计。核心规则：

- **LLM 只能输出两个字段：`veto`（布尔值）和 `veto_reason`（字符串）**，Prompt 里显式声明
  "不得输出方向/价格/仓位"，输出经过 Pydantic 校验，只要格式不对（比如输出里混进了价格数字）
  就直接判定这次调用失败。
- **输入是结构化对象，不是原始网页/社媒文本拼接**——喂给 LLM 的是策略核心假设、技术信号列表、
  融合结果、MetaLabel 结果、近期风险事件、波动率上下文，以及从本地 RAG 检索出的最多2条相关策略
  笔记摘要。
- **Fail-closed（宁可错过机会，不可放宽风控）**：
  - 超时 → 判定为否决（不是"当作没意见"）。
  - 每日调用预算超限（`decision_veto_daily_budget`，默认200次/天）→ 判定为否决，并写入
    高优先级通知。
  - 输出格式校验失败 → 判定为否决。
  - 这三种情况都会完整记录 `AgentTask` 失败事件，供你复盘时区分"LLM主动否决"和"因故障被迫否决"
    两种不同性质的否决。
- **Provider 容灾链**：Anthropic 优先，失败或限流会自动降级尝试 OpenRouter 免费模型池、
  GitHub Models 免费模型池（两者都会自动发现当前可用的免费模型列表并缓存 6 小时），
  这个设计能在 Anthropic 服务波动时提高否决层的可用性，但也意味着**否决判断的模型质量在不同时刻
  可能不一致**（有时是 Claude 判断，有时可能降级到免费小模型判断），这是需要你知道的一个隐性
  风险点。

### 3.7 Gatekeeper 最终风控关口

在 LLM 否决通过之后，还有最后一道**纯规则、不受LLM影响**的关口：

- 无止损计划 → 直接拒绝（除非是平仓单）。
- Validation 回测未通过准入 → 拒绝。
- 数据不新鲜（超过配置的新鲜度阈值）→ 拒绝。
- 存在阻断性风险事件（比如交易所暴雷级别的 critical 事件）→ 拒绝。
- Kill Switch 被触发（全局熔断）→ 无条件拒绝所有新单。
- 数值风控：单品种/总敞口超限、最大同时持仓数超限、杠杆超限、日/周亏损限触发、回撤熔断触发、
  连续亏损次数触发、**Martingale检测**（亏损连续期内下单金额超过现有该品种敞口2倍会被判定为
  变相加倍下注）、API失败次数超限——任意一项触发都拒单。

这道关口和 LLM 否决是**并行独立**的两道关卡，不是谁替代谁——即便 LLM 判定"不否决"，只要
Gatekeeper 的规则不过，订单照样会被拒绝。这个设计正确地保证了"LLM是锦上添花的一层保守过滤，
不是最终裁决者"。

### 3.8 Binance-first 下单语义

如果启用了 Testnet mirror 模式（`binance_auto_execute=true`），开仓和平仓都会先提交到 Binance
Gateway，只有 Binance 那边确认成功后，本地才会记录 fill/持仓变化。如果 Binance 那边失败，
本地**绝不会**先开/先平，订单会被标记为 `rejected` + `gateway_status=gateway_failed`，避免
"本地账本以为开仓成功，但交易所那边其实没成交"这种状态分叉问题。这个设计是对的，也是你目前
"模拟盘已经跑通"的技术前提。

---

## 四、平单逻辑详解

平仓判断的优先级（严格按此顺序）：

1. **保护性止损/止盈**：拿最近一笔已成交入场单当时定下的止损/止盈价格计划，用当前 K 线的
   最高价/最低价去判断是否触及（不是用收盘价，是用整根K线的高低点判断，更贴近真实盘中触发逻辑）。
2. **同一根K线同时触及止损和止盈时，优先按止损处理**——这是一个偏保守的裁决，宁可"少赚一次"，
   不承担"到底先触发哪个"的不确定性风险。
3. **移动止损（Trailing Ratchet）**：当价格向盈利方向运行超过设定的 R 倍数（`trail_after_r`，
   R = 初始止损距离）后，把止损收紧到入场价，且**只收紧，永远不放松**——多头方向止损只会往上移，
   空头方向止损只会往下移，这是标准的"保本+让利润奔跑"逻辑。
4. **反向信号平仓（不直接反手）**：当新一轮出现与当前持仓方向相反的信号，且策略配置
   `close_on_opposite_signal=true` 时，系统会先生成一个"只平仓不新开"的平仓单（`close_only_mode`），
   **不会在同一轮里直接反手开反向仓位**——这意味着如果你想要"多转空立即开空"的效果，目前需要等
   下一轮扫描重新走一遍完整开单链路，不是平仓的同时自动反手。
5. **没有新信号时**，已有仓位继续由第1~3步的保护性逻辑管理，不会因为"没信号了"就主动清仓。

平仓同样遵循 Binance-first 语义——如果启用了 Testnet mirror，平仓也要先过 Binance Gateway，
失败则本地不平仓。

---

## 五、仓位管理详解

### 5.1 名义金额（Notional）计算公式

优先级从高到低：

1. 如果策略直接配置了固定名义金额（`notional_usdt` 或 `order_notional_usdt`），直接用固定值
   （乘以置信度系数）——这种模式适合你想手动固定每单金额时使用。
2. 否则走**风险预算模式**（`risk_per_trade`，这是通道二的默认模式）：
   ```
   风险预算金额 = 账户权益 × risk_per_trade（默认1%）
   止损距离    = |参考价 - 止损价|
   目标数量    = 风险预算金额 / 止损距离
   波动率定价名义金额 = 目标数量 × 参考价
   最终名义金额 = min(波动率定价名义金额, 账户权益 × max_position_fraction（默认5%）× max(杠杆,1))
   最终下单金额 = 最终名义金额 × 置信度乘数（融合置信度 × MetaLabel下注比例）
   ```
3. 都没配置时，退化为"账户权益的5%，且不超过1000 USDT"的保守默认值。

**这个公式的核心思想是"止损距离越远，同样风险预算下下单数量越小"**——也就是常说的"波动率定价
仓位"，与你可能在其他系统里见过的"固定手数""固定金额"这种粗放方式相比，是更专业、更能自动适应
不同品种/不同波动率环境的做法。

### 5.2 止损/止盈价格计算

- 止损距离：优先用策略配置的固定基点(bps)，否则用 ATR × 倍数（默认1.5倍），都没配置则退化为
  参考价的1.5%。
- 止盈距离：优先用固定基点，否则用"止损距离 × 风险回报比"（默认2倍，也就是常见的1:2 R:R）。

### 5.3 风险状态快照（喂给Gatekeeper数值风控用）

每次下单前会构建一份 `ExecutionRiskState`：账户权益、权益峰值（算回撤用）、当日/当周已实现盈亏、
连续亏损次数、API失败次数、当前持仓数、该品种敞口占比、总敞口占比、本次请求名义金额、本次请求杠杆
——这份快照就是第三章 Gatekeeper 数值风控判断的直接输入。

### 5.4 仓位管理"尚需加强"的部分（文档自己也承认）

- **实时账户权益与 Binance Testnet 仓位的同步机制还不完整**——目前很多地方是从
  `paper_run.paper_metrics_summary` 里取账户权益，是否与 Binance 那边的真实余额做过严格核对，
  需要你在实际跑的时候重点验证。
- **没有单品种总风险、相关性风险、组合净敞口的概念**——如果你同时在多个交易对上跑同一套技术
  信号逻辑，系统目前不会意识到"这几个仓位其实高度相关，实际风险远比账面敞口数字看起来大"。
- **没有基于订单簿深度/OI的动态滑点和最大下单量限制**——现在的下单量计算完全基于账户权益和
  止损距离，不会考虑"这个品种当前流动性够不够吃下这么大一单"。

---

## 六、LLM + RAG 详细拆解

### 6.1 LLM 的职责边界（这是全系统最核心的不可谈判原则）

**允许 LLM 做的事**：
- 对新闻/宏观/风险事件做严重度分类（`classify_event`）。
- 对信号融合结果做执行前二元否决（`pre_execution_veto_llm`）。
- 对复盘失败原因做总结（目前这一步实际是规则聚合，未真正接入LLM，见报告一第3.6节）。
- 从策略库笔记和开源研究资产里检索上下文（RAG）。

**明确禁止 LLM 做的事**：
- 直接输出买卖方向、价格、仓位大小、杠杆。
- 绕过 Gatekeeper、Validation 或 Review 的既定规则。
- 因为 LLM 服务不可用而自动放宽风控阈值。
- 修改已生成信号的内容（比如把方向从 long 改成 short），只能对整个信号做否决/不否决的二元判断。

这套边界的设计意图很清楚：**LLM 在这个系统里的角色定位是"额外的保守过滤层"，不是"决策者"**。
这与很多"AI自动交易"项目里让 LLM 直接分析行情说"我觉得该买"的做法完全不同，也是这个项目
风控上比较让人放心的一点。

### 6.2 当前 RAG 的真实实现形态（这一点你需要有清晰认知）

你在问题里提到"LLM加RAG如何辅助开平单"，这里有个重要的现实需要说明：**目前的 RAG 不是你可能
设想的"向量数据库+语义检索"，而是一个纯关键词匹配的本地检索**。具体逻辑：

1. 检索范围：`策略库/*.md`（也就是你项目里的策略笔记文档）+ 8个开源项目（Freqtrade/Jesse/
   Hummingbot/ABU/NautilusTrader/Qlib/vectorbt/OpenBB）摄取下来的本地 Markdown 摘要。
2. 检索方式：把当前信号的核心假设、入场规则、交易对名称，用正则切分成词，再补充一批固定关键词
   （macd/funding/carry/trend/breakout/fake_breakout/rsi/adx/ema/vwap/bollinger/资金费率/套利/
   趋势/假突破/假跌破/缠论/买卖点/风控），和每份文档的预览文本做**关键词重合度打分**，取分数
   最高的最多2条，每条只截取240个字符的预览。
3. 这2条检索结果会作为 `rag_snippets` 字段塞进 Decision Veto Agent 的 Prompt 里，作为额外上下文。

**这意味着什么**：
- 检索精度依赖固定关键词表和简单分词，**没有语义理解能力**——比如你的策略假设如果用了一个
  关键词表里没有的同义词，可能检索不到本该相关的笔记。
- 每次最多只给2条、每条240字的极短预览，**上下文深度非常浅**，本质上更像是"给 LLM 一点点
  背景提示"，而不是真正意义上的"检索增强生成"。
- 好处是：**没有额外的向量库基础设施依赖，实现简单、延迟低、成本可控**，对于当前"LLM只做二元
  否决"这种低风险敞口的用途，这个简化版RAG基本够用，不会因为检索质量差就产生错误的执行决策
  （因为LLM本身也只能输出二元否决，检索内容质量差顶多影响否决理由的解释力，不会直接影响
  仓位/方向）。

如果未来你想让 RAG 承担更重的角色（比如辅助 Strategy Agent 做真正的自然语言假设规则化、
或者辅助 Review Agent 做归因分析），现在这套关键词检索会明显跟不上，需要升级为真正的向量检索
（文档里也承认了这一点，"向量数据库/LlamaIndex 检索层"被列为未完成项）。

### 6.3 Market Intelligence：另一条"数据影响决策"的路径，但不是LLM否决

容易和 LLM+RAG 混淆的一点是：新闻/宏观/链上数据(CoinGlass/CryptoQuant/DeFiLlama)对交易决策的
影响，走的是**另一条独立路径**——它们被归一化成一个"受限投票"（`market_intelligence`），
和8个技术信号一起参与 SignalEnsemble 的加权投票，但权重被硬性限制在30%以内（防止市场情报信号
喧宾夺主，覆盖掉技术信号本身的判断）。这条路径**完全不涉及LLM**，是纯数据+规则的路径。

而 News/Twitter Agent 对新闻严重度的分类，才是真正用到 LLM 的地方——但它们的输出只会转成
`RiskEvent`（风险事件），走既定的风控分级触发规则（比如高严重度新闻触发30分钟暂停新开仓+
止损收紧70%），**不会被News Agent直接映射为"该开仓/该平仓"的指令**。

### 6.4 LLM 调用的容灾与成本控制

- **Provider 优先级**：Anthropic → OpenRouter免费模型 → GitHub Models免费模型，任何一个出现
  401/403/429或5xx错误都会自动尝试下一个候选。
- **每日预算硬顶**：`decision_veto_daily_budget` 默认200次/天，超限直接判定为否决并发通知，
  避免信号频率突增时LLM调用成本失控。
- **不对所有候选信号调用**：只有通过了信号融合+MetaLabel下注判定的信号才会触发一次LLM调用，
  大部分被过滤掉的候选信号根本不会消耗LLM调用次数——这是成本控制上合理的设计。

---

## 七、与你人工交易经验的对照总结

| 你的经验要素 | 系统现状 | 结论 |
|---|---|---|
| 4h看多空方向 | `direction_timeframe=4h`，多周期确认机制强制15m信号方向与4h一致 | ✅ 已完整实现 |
| 15m假突破/假跌破入场 | `generate_false_breakout_signal`，逻辑与你描述的手法基本一致 | ✅ 已实现 |
| MACD金叉死叉 | `generate_macd_signal`，标准12/26/9参数+histogram动量补充 | ✅ 已实现，且更连续 |
| RSI超买超卖 | `generate_rsi_signal`，要求"穿越"而非"处于"超买超卖区，避免误用 | ✅ 已实现，且更严谨 |
| 缠论买卖点/笔/线段/中枢/背驰 | **完全未实现**，文档明确要求先定义客观算法才能加入 | ❌ 缺口，见第3.2节的建议路径 |
| （你未明确提及，但系统额外加入的） | Dow结构、ADX强度过滤、EMA趋势、VWAP、布林带 | ➕ 系统在你经验基础上做的合理扩展 |
| （你未明确提及，但系统额外加入的） | 新闻/宏观/链上数据受限投票（Market Intelligence） | ➕ 額外维度，默认部分依赖未配置的第三方API Key |

**总体判断**：你原有的核心交易逻辑已经被相当忠实地代码化，且系统在此基础上补充了更完整的多指标
体系和更严格的多重确认机制（相关性过滤、MetaLabel历史胜率过滤、LLM否决）。**唯一明显的功能缺口
是缠论买卖点**，这不是技术能力不足，而是团队/系统主动选择"先把客观算法定义清楚再实现"，避免把
一个存在多流派争议的方法论仓促代码化后污染整个"规则化可回测"的体系。这个态度本身是对的，
建议你在下一步优化时，把"定义你自己认可的那一套缠论客观判定标准"作为一个独立的前置任务来做。

---

## 八、可以从已参考的开源项目中继续吸收的内容

仓库的 `策略库/01_外部策略来源索引.md` 已经对 Freqtrade/Hummingbot/QuantConnect Lean/vn.py/
ABU 等项目做了"可吸收内容"梳理，以下是我核对代码后，认为**还没有真正落地、但确实值得继续吸收**
的具体点：

| 来源 | 可吸收但尚未落地的内容 | 建议 |
|---|---|---|
| Freqtrade | Lookahead analysis / recursive analysis（系统性检测策略是否存在未来函数或指标不稳定） | 当前只是靠人工代码规范（比如WS忽略未收线K线）来避免未来函数，没有自动化检测工具，建议后续给回测层加一个自动lookahead检测步骤 |
| Hummingbot | 库存偏斜（inventory skew）、挂单生命周期管理、撤单超时 | 对应S6 Grid/做市策略，目前明确P2延后，暂不需要 |
| vn.py | Gateway/CTA策略/Portfolio/Risk分层思想 | 当前Binance-only场景下暂不紧迫，但如果未来要扩展到其他交易所/资产类别，这个分层思路值得参考 |
| ABU（蒸馏研究，非代码） | 价格行为、趋势跟踪的规则化提炼 | 已在RAG资产库中，但检索深度浅（见6.2节），建议后续用得上时手动通读原始蒸馏笔记，而不是完全依赖关键词检索 |
| QuantConnect Lean | 事件驱动引擎、多资产组合层 | 当前不引入其runtime，仅供架构思想参考，暂不紧迫 |

---

## 九、优化建议清单（按对你当前"优化量化策略"目标的优先级排序）

### 高优先级（直接影响策略质量和你最关心的开平单效果）

1. **补齐缠论客观算法**：这是你明确提出、系统明确缺失的一块。建议先用一小段历史数据，手动标注
   你认可的买卖点位置，再选定一套客观分型/笔/线段算法，验证吻合度后再接入信号池（具体路径见3.2节）。
2. **让4h确认周期和15m入场周期使用不同的信号子集**：目前两个周期用的是同一组8个信号函数，
   建议让4h周期更偏重结构类信号（Dow结构、EMA趋势、ADX），15m周期更偏重反转/入场类信号
   （假突破、pin bar、RSI穿越），更贴近你原有的分工习惯。
3. **给MetaLabel引入更长、更严谨的样本外验证**，而不是只用最近47根K线现算胜率——可以考虑把
   已经在 Validation Layer 实现的walk-forward/OOS框架复用到MetaLabel的胜率估计上，而不是两套
   逻辑各自为战。
4. **补齐单品种/组合层面的相关性风险和净敞口计算**：如果你同时在多个交易对上跑同一套逻辑，
   目前系统不会意识到实际风险的相关性叠加。

### 中优先级（提升可解释性和研究效率）

5. **把Review Agent真正接入LLM**，让每日复盘报告从"失败类型去重+固定模板建议"升级为有真实
   归因分析的报告——这对你判断"策略是该继续、调参、暂停还是淘汰"会有实质帮助。
6. **评估RAG是否需要升级为向量检索**：如果你打算持续扩充策略笔记和开源研究资产的数量，现在
   的关键词匹配检索深度会越来越不够用。
7. **给RSI/布林带均值回归策略（候选库S4）补充regime filter**：文档自己也提到"需要regime filter，
   否则容易在趋势里连续亏损"，目前ADX只是作为独立信号存在，没有被用作"是否允许均值回归信号
   生效"的开关。

### 低优先级（等条件成熟后再做）

8. VWAP/S5策略接入订单簿深度和OI数据，让入场判断更精细。
9. Grid/做市策略（S6）等订单簿、成交回报、撤单状态更完整后再启动。
10. 补齐交易所API Key权限自检（这个更偏运维安全，不直接影响策略效果，但在你考虑真实资金前必须做，
    详见报告一第七章）。

以上就是当前系统在"量化策略、开单/平单逻辑、仓位管理、LLM+RAG"四个维度的完整拆解。如果你需要，
我可以针对第九章里任何一条优化建议，进一步给出具体的实现方案或代码改造思路。

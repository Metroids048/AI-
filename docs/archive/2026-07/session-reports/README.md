# 2026-07 session delivery / diagnosis reports

Moved from `docs/` root during 2026-08 structure cleanup. Historical only —
do not treat as current runtime truth. See `docs/README.md` and `CURRENT_STATE.md`.

Command examples inside these reports may still mention `scripts/<name>.py`.
Many one-off ops scripts now live under `scripts/archive/2026-08-ops-checks/`.
Supported entry points remain in `scripts/SUPPORTED.md`.
